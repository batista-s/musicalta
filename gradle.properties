# Use Java 25 for Gradle execution so the build and wrapper use the team's JDK
# Update this path if your team's JDK 25 is installed somewhere else.
org.gradle.java.home=/usr/lib/jvm/java-25-openjdk

